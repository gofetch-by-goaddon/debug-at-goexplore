{
  "update_config": {
    "klass": "webshops",
    "query": {
      "company_id": "{{ company_id }}",
      "_id": "{{ _id }}"
    },
    "document": {
      "config": "{{ config | json }}"
    }
  },
  "reset_got_at_attr": {
    "klass": "webshops",
    "query": {
      "company_id": "{{ company_id }}",
      "_id": "{{ _id }}"
    },
    "document": {
      "{{ got_at_attr }}": null
    }
  }
}