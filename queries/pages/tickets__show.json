{
  "tickets": {
    "klass": "tickets",
    "query": {
      "company_id": "{{ company_id }}",
      "_id": "{{ _id }}"
    }
  },
  "auths": {
    "klass": "auths",
    "query": {
      "_id": "{{ _id }}"
    }
  }
}